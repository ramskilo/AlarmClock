{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug AlarmClock",
      "type": "python",
      "request": "launch",
      "program": "${workspaceFolder}/src/alarmclock/main.py",
      "console": "integratedTerminal",
      "cwd": "${workspaceFolder}",
      "env": {
        "PYTHONPATH": "${workspaceFolder}/src"
      }
    }
  ]
}